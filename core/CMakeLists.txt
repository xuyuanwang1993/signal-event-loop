
list(APPEND INCLUDEPATHS ${CMAKE_CURRENT_SOURCE_DIR} )
file(GLOB_RECURSE LOCAL_SOURCE ./*.cpp)
file(GLOB_RECURSE LOCAL_HEADER ./*.h)
list(APPEND HEADERS ${LOCAL_HEADER})
list(APPEND SOURCES ${LOCAL_SOURCE})
set_parent_values()